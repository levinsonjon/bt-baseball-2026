- **2026-06-17** — Triaged "job didn't run today." ROOT CAUSE: same empty-DATA-draft failure — the remote agent created an empty `Fantasy Baseball DATA 2026-06-16` draft (no `<script>` JSON blocks) AND never created the email draft at all (run died mid-step-10), so `send_pending_email.py` ran fine at 5:34am but had nothing to sync and correctly fired the empty-draft Reminder; site froze at 6/15. (1) **Caught the site up**: rebuilt `yesterday.json`/`news.json`/`season_stats.json` for 6/16 from the **MLB Stats API gameLog** (deterministic, no web search) — 8 played (Ben Rice HR/2RBI/2R, PCA HR, Reid Detmers 7 scoreless IP/W/3.9pts; Arraez sat, Teoscar on IL). Resolved missing swap IDs (Basallo 694212, McGonigle 805808, Rice 700250). Committed `4f0f49e`, pushed, verified HEAD==origin/main; trashed the empty 6/16 draft (gmail-personal lacks delete scope, used TRASH label). (2) **Interim hardening of remote trigger** `trig_01AWGDMAqyJY5oZNYiqKQQdT` via `RemoteTrigger update`: cut WebSearches 4→2 per player (64→32), bumped model Sonnet-4-6 → **Opus-4-8**, added a "Run discipline — finishing both drafts beats perfect prose" preamble. Diagnosis: the prompt was already maxed on assemble-then-verify guards (added 6/16) yet still failed — the run dies before reaching step 10, so the fix is to shorten/strengthen the run, not add more guards. ⚠️ if the 6/18 ~4:31am run errors entirely, revert model to `claude-sonnet-4-6`. (3) **Wrote `local-pipeline-plan.md`** (approved design, not built): durable fix = retire the remote trigger and generate locally off the MLB Stats API + ESPN injuries + a thin Claude API news call (news degrades gracefully to templated box-score summaries). ~80% reuses existing scoring/email/push code. Jon will execute later — no deadline.
- **2026-06-16** — Re-authed the `gmail-personal` MCP OAuth token (expired since 5/12, `invalid_grant`). The long re-auth command kept breaking at `npx` due to terminal line-wrapping; wrapped it in `~/.config/personal-mcp/gmail/reauth.sh` so future weekly re-auths are one short line (`! bash .../reauth.sh`). New token written with `gmail.modify` scope; verified the connector returns live mail. Killed stray auth processes from the failed attempts.
- **2026-06-16** — Diagnosed and fixed the daily pipeline stall (frozen since 5/31). ROOT CAUSE: the remote agent's DATA drafts since ~6/2 arrived with an **empty body** (subject correct, body just `\n\n\n\n`, no `<script type="application/json">` blocks), so `send_pending_email.py` correctly found nothing to sync and the website froze at 5/31. The EMAIL drafts kept working (daily emails still arrived), masking the failure. The `update_health.py` freshness guard DID fire a "PIPELINE STALE" Reminder every morning for 15 days (41 times) — but it **misdiagnosed** the cause as "Likely Gmail OAuth revoked," sending the wrong fix; OAuth was fine. (1) **Caught the site up**: rebuilt `yesterday.json`/`news.json`/`season_stats.json` for 6/15 via parallel web research on all 16 players (8 played, 3 injuries — matches the 6/15 email); season_stats anchored on the trustworthy frozen 5/31 base (swapped slots += reliable June 1-15 game-log ranges; Teoscar/Ragans on IL kept frozen; non-swapped += confident 6/15 line, rest self-heals on next clean run). Caught agent data errors (Ragans phantom IL-period starts, Bichette R:0). Committed `f6201bc`. (2) **Hardened the remote trigger prompt** (`trig_01AWGDMAqyJY5oZNYiqKQQdT`) via `RemoteTrigger update`: step 10 DATA draft is now assemble-then-verify (build JSON, confirm all three populated `<script>` blocks + non-trivial body size before `create_draft`, never ship an empty/skeleton draft), cap news to 1 source/player. Run env preserved (Sonnet 4-6, tools, connector). (3) **Added cause-specific local detection**: `consume_data_draft()` now returns a status; when DATA drafts exist but yield zero JSON blocks and nothing syncs, `send_pending_email.py` fires a distinct "remote agent producing EMPTY data drafts" Reminder (NOT the OAuth one), and `update_health.py`'s stale message now lists empty-drafts FIRST, OAuth second. (4) Deleted the 13 accumulated empty DATA drafts (6/02-6/15) from Gmail — all confirmed empty. Next remote run ~4:31am ET 6/17 will validate the prompt fix. Side note: `gmail-personal` MCP token expired (`invalid_grant`), unrelated, needs re-auth.
- **2026-06-16** — Reverted the armed Bo Bichette → Miguel Vargas (3B) swap — Jon confirmed Bichette is NOT actually on the IL, so the substitution is unwarranted. The one-shot swap had only ever run in dry-run mode (6/15); the live job was scheduled for 23:00 ET Sun 6/21 and never fired, so `data/my_team.json` (still drafted-player "Bo Bichette", no `current_player`) and `season_stats.json` were untouched — a clean disarm with no data to roll back. Booted out the `com.jon.fantasy-baseball-swap` launchd job, deleted `~/Library/LaunchAgents/com.jon.fantasy-baseball-swap.plist`, and removed `tools/swap_bichette_to_vargas.py`. The OF swap (`com.jon.fantasy-baseball-swap-of`, Teoscar → Chourio, fires Mon 6/22) is unaffected and remains armed.
- **2026-06-15** — Ran `/bt-substitute` for the OF slot (Teoscar Hernández, on the 10-day IL since 5/27 with a hamstring strain) and armed a one-shot swap to Jackson Chourio (MIL) effective Mon 2026-06-22. FA analysis: Chourio's blended BT (496) topped the OF free-agent pool, +29 over Teoscar (467), with elite multi-cat value (24 HR / 87 R / 21 SB projected); healthy (back from a L-hand fracture, activated 5/4, multi-HR game 6/4) — his low 152 AB is the injury, not bench time. Brent Rooker (next-best, +2) just hit the IL 6/12 (knee) and is a projection mirage on a .200 start; Bryan Reynolds (−1) flagged as the safe-floor lateral. Newly available via 6/15 BBSUBS: Roman Anthony (dropped by Cobey 5/18). Built `tools/swap_teoscar_to_chourio.py` mirroring the Bichette→Vargas script but targeting the slot BY NAME ("Teoscar Hernandez") since three OF slots exist; OLD_CURRENT_PLAYER guard expects the accented "Teoscar Hernández". DISTINCT launchd label `com.jon.fantasy-baseball-swap-of` so it doesn't collide with the Bichette→Vargas job (`com.jon.fantasy-baseball-swap`, fires Sun 6/21 and self-removes). Scheduled by `com.jon.fantasy-baseball-swap-of.plist` for 23:00 ET Mon 6/22 (Month=6/Day=22/Hour=23), BEFORE the Tue 6/23 4:35am remote run. EXPECTED_AS_OF=6/21. Carryover into `Teoscar/Chourio`: AB=152 / H=41 / .270 / 7 HR / 34 RBI / 24 R / 1 SB (frozen since Teoscar's 5/27 injury, so accurate despite pipeline staleness). Dry-run (`--ignore-freshness`) confirmed clean diff; freshness guard verified blocking; plist verified armed. Same ⚠️ pipeline caveat as the Vargas swap: stalled since 5/31, must be revived or neither swap fires. Committed the script for disaster recovery.
- **2026-06-15** — Ran `/bt-substitute` for the 3B slot (Bo Bichette) against the fresh 6/15 BBSUBS, then armed a one-shot swap to Miguel Vargas (CHW) effective Sun 2026-06-21. FA analysis: only Vargas's blended BT (443) topped Bichette (439, +4) among 3B-eligible free agents — healthy everyday player, 16 HR / 51 R / 10 SB through mid-June; Donovan and Suárez looked good on paper but are injury/collapse mirages. Mudge dropped Eugenio Suárez (4/27) back into the pool; no new 6/15 adds affected the 3B pool (only Max Muncy, already rostered). Built `tools/swap_bichette_to_vargas.py` mirroring the Jeffers→Basallo pattern (pre-flight guards, idempotency, freshness guard, fetch+rebase+push, confirmation email via `gmail-fb` creds, launchd self-uninstall); the 3B slot starts as drafted-player "Bo Bichette" (no `current_player`), so OLD_CURRENT_PLAYER guard expects None. Scheduled by `com.jon.fantasy-baseball-swap.plist` to fire once at 23:00 ET on Sun 6/21 (Month=6/Day=21/Hour=23), BEFORE the Mon 6/22 4:35am remote run, so Vargas's Sun 6/21 box score lands in the Mon 6/22 email. EXPECTED_AS_OF=6/20. Slot relabels to `Bichette/Vargas`, `current_player` Miguel Vargas, zeroed projections. Dry-run (`--ignore-freshness`) confirmed clean diff; freshness guard verified blocking today; plist verified armed via `launchctl print`. ⚠️ FLAGGED: daily pipeline has been stalled since 5/31 (`yesterday.json` at 2026-05-31, no daily-update commits in 15 days) — the freshness guard will REFUSE to fire the swap Sunday unless the pipeline is revived (likely revoked `gmail-fb` token or stalled remote agent). Committed the script for disaster recovery.
- **2026-06-01** — Manually completed the Ryan Jeffers → Samuel Basallo (C) swap after the scheduled launchd job failed. The `com.jon.fantasy-baseball-swap` job fired the night of 5/31 but its pre-flight guard correctly refused to swap: the daily pipeline was lagging, so `yesterday.json` was still at 5/29 (guard requires ≥ 5/30) — `launchctl` showed exit status 1, plist did NOT self-uninstall. Catcher slot remained `Raleigh/Jeffers` and Basallo was never swapped in. Since the plist's StartCalendarInterval (5/31) had passed, launchd would never re-fire it. By 6/1 the pipeline had caught up (`yesterday.json` at 5/31), so re-ran `tools/swap_jeffers_to_basallo.py`: dry-run confirmed clean diff, then live run wrote `my_team.json` + `season_stats.json` (slot → `Jeffers/Basallo`, `current_player` Samuel Basallo, carryover AB=164 / .159 / 7 HR / 18 RBI / 16 R / 2 SB), committed `ef7cedf`, pushed to origin/main (Vercel redeploy), sent confirmation email, and self-removed the plist. One benign warning: the script's `git rebase` step aborted on the pre-existing unstaged `tasklog.md` change, but the push fast-forwarded cleanly (remote hadn't advanced past `f68c9ee`) — verified local == origin/main == `ef7cedf`.
- **2026-05-26** — Re-ran /bt-substitute for the C slot after Ryan Jeffers landed on the IL (hamate fracture, surgery, 6-8wk + power-sapping aftermath). Health-checked the top FA catcher pool: Agustín Ramírez (top projection BT 433) was optioned to AAA 5/4; Alejandro Kirk (TOR, BT 420) still rehabbing post-thumb-surgery with mid-June MLB return; Yainer Diaz (HOU, BT 418) just placed on 10-day IL with oblique strain; only Dingler, Basallo, and Carson Kelly are both healthy and playing every day. Built scenario-based ROS projections for Kirk/Dingler/Basallo accounting for the 300-AB minimum penalty — Kirk's mid-June-return base case projects only BT ~283 (even optimistic case ~373) because his 20 AB banked + ~220 ROS AB triggers the 300-floor penalty; Basallo base 393, optimistic 412; Dingler base 379. Decided to swap to Samuel Basallo (BAL). Verified C eligibility under BBRULES 2026 §replacement-position-assignment: per MLB Stats API, Basallo has played 23 G at C (most), 16 at DH, 4 at 1B in 2026 — clears the "most-games" test for C-slot eligibility. Then armed `tools/swap_jeffers_to_basallo.py` (mirrors the Stanton→Rice pattern: pre-flight guards, idempotency, fetch+rebase+push, confirmation email via `gmail-fb` creds, launchd self-uninstall) scheduled by `com.jon.fantasy-baseball-swap.plist` to fire once at 23:00 ET on Sun 2026-05-31. Timing rationale: fires BEFORE the Mon 6/1 4:35am remote-agent run (instead of the 5/18-pattern post-cron 6:30am fire), so the agent reads the swapped `my_team.json` and pulls Basallo's Sun 5/31 box score into the Mon 6/1 morning email. EXPECTED_AS_OF guard set to 2026-05-30 (Sun 5/31 5:30am cron must have committed Sat 5/30 data before the swap fires). Carryover totals into `Jeffers/Basallo` composite: AB=164 / H=26 / .159 / 7 HR / 18 RBI / 16 R / 2 SB. Dry-run confirmed clean diff; plist verified armed via `launchctl print`. Committed as `674ea06` and pushed to origin/main for disaster recovery.
- **2026-05-18** — Cal Raleigh → Ryan Jeffers C-slot swap, plus pipeline-bug recovery from agent schema drift. Used `/bt-substitute` to evaluate C FAs; discovered Raleigh hit the 10-day IL with right oblique strain (multi-week absence). Picked Ryan Jeffers (MIN, .302/.409/.531) — cleanest healthy pickup. Built one-shot script `tools/apply_swap_jeffers.py` to do the standard composite-name + season_stats carryover edit. Then recovered the Mon 5/18 5:30am cron, which had crashed on two agent schema drifts in the 5/17 DATA draft: (1) `yesterday-data` block came back as a bare list instead of the `{date, generated_at, totals, players}` dict — `normalize_yesterday` crashed on `.get("players")` and the watermark check then crashed on `.get("date")`, leaving raw payload on disk uncommitted; (2) `news-data` per-player entries renamed injury fields to `injury_flag`/`injury_curr`/`injury_prev`/`injury_note` (was `injury_status`/`headline`), so the news normalizer's fallback found zero injuries even with Raleigh + Ragans clearly injured. Fix (commit `1ee206f`): made `normalize_yesterday` accept bare list input (mirrors `normalize_news` shape-guard from 5/4), derives date/generated_at and computes totals from per-player stats; extended `normalize_news` to also read the new injury field names; hardened `current_data_watermark` and `consume_data_draft` against on-disk/in-block list shape. Drained backlog: re-ran `send_pending_email.py`, hand-regenerated `news.json` from the draft body (since the script had already written raw payload, then the second bug stopped it before commit/push), pushed `Daily data update 2026-05-17` (`566c8db`), sent the 5/17 email, deleted the DATA draft via `gmail-fb` creds (the gmail-personal MCP lacks delete scope). Finally applied the swap (`a65d0f9`): `Raleigh/Jeffers` composite with `current_player: "Ryan Jeffers"`, carryover 161 AB / .161 / 7 HR / 18 RBI / 16 R / 2 SB. Tuesday 5/19 agent run will pick up Jeffers's Mon 5/18 game.
- **2026-05-12** — Recovered fantasy baseball pipeline after 4-day silent outage (May 8-12), then implemented the deferred OAuth client separation to prevent recurrence. Symptom: site stuck on May 7 data, no daily emails since the May 7 send. Root cause (same as 5/1): local Gmail OAuth refresh token revoked 5/9 05:53 ET when the cron script and the `gmail-personal` MCP server raced on token refresh — third mid-week revocation in 12 days. Compounding: the macOS Reminder fallback timed out on every failed run (osascript 10s ceiling too tight for cold-wake cron), so no alert ever fired across 4 days. Recovery: re-authed Gmail; ran `send_pending_email.py` once — drained queued May 8/9/11 DATA drafts (no May 10 draft ever produced — remote agent skipped that day) and sent the May 11 email. Backfilled swapped-slot stats from MLB Stats API for the 5/8-10 gap (each agent run during the outage read the stale May 7 repo baseline and OVERWROTE swapped totals with [baseline + that day's box] instead of accumulating, so the final state was [May 7 + May 11 only]): McGonigle +AB 10, +H 2, +R 1, +SB 1; Rice +AB 13 / 0-fer. Non-swapped slots unaffected (agent web-sources their totals). Permanent fix (the proposal saved to memory after the 5/1 outage): created a second OAuth 2.0 Client ID under the same GCP project `personal-claude-mcp-486922`, type "Desktop app", named "Fantasy baseball local script"; saved keys to `~/.config/personal-mcp/gmail-fb/`; ran one-time `npx server-gmail-autoauth-mcp auth` flow against the new client; patched `send_pending_email.py` and `update_health.py` constants to point at `gmail-fb/`. The cron scripts and the gmail-personal MCP server now have independent refresh tokens and can't race each other. Weekly token expiry from Testing-mode still requires a re-auth (see `feedback_gcp_production.md` — sensitive scopes mean Production is gated behind Google review). Also bumped the osascript timeout from 10s to 60s in both scripts so the freshness Reminder actually fires on the next failure. Updated CLAUDE.md with new paths and re-auth command.
- **2026-05-06** — Extended `/bt-substitute` skill to support DH, SP, and RP positions. DH uses unfiltered hitter pool (all hitters DH-eligible per league rules). SP scoring uses RSAR formula `(1.2 × MLB_AVG_ERA − ERA) × (IP/9)` with IP/G ≥ 3.5 reliever-prevention penalty; ranks per-player RSAR (×3.5 multiplier is team-level only). RP scoring is `5 × (W + SV)`. Switched API endpoint to `group=pitching` for both. Added pitcher-specific gotchas to skill: MLB API `inningsPitched` string format (`"42.1"` = 42⅓), two-way players (Ohtani — check both `player_type` lists), SP per-game ERA volatility, and closer-churn for RP (always WebSearch current bullpen role for top RP candidate).
- **2026-05-06** — Free-agent analysis to replace Teoscar Hernández in OF slot. Built FA pool from draft tracker (Google Sheet `1BMZk1b3IFyek48tngRlK_FpqMGErgKpuFjEe7jwjQcE`) + BBSUBS swap log; pulled 2026 YTD stats from MLB Stats API; ranked OF-eligible FAs by projected season-end BT total (YTD actuals + BoS-rate ROS, 300-AB floor). Top 5: Brent Rooker (BT 482, Δ+35; just activated from oblique-strain IL — health-clean regression bet), Jackson Chourio (472, +25; tiny 4-AB sample), Christian Yelich (461, +14; .314 in 51 AB at drop), Bryan Reynolds (454, +7; cleanest never-rostered add), Taylor Ward (446, −1). Encoded the workflow as the `/bt-substitute` skill at `~/.claude/skills/bt-substitute/SKILL.md` for reuse on future positions/dates; saved a reference memory pointing at the data inputs.
- **2026-05-05** — Recovered fantasy baseball pipeline after 5:30am cron hit DNS-outage failure (3rd morning in a row); manually drained May 04 DATA draft, pushed daily data update, sent May 04 email
- **2026-05-04** — Recovered fantasy baseball pipeline after another 2-day silent outage. Symptom: no email Sat or Sun, website still showing 5/01 data. Root cause: DNS failure (`Unable to find the server at gmail.googleapis.com`) at the 5:30am launchd cron on both 5/03 and 5/04 — Mac was likely just-woken when the job fired. Different from the 5/01 OAuth-revocation outage; this was pure transient network. Two compounding bugs surfaced: (a) the freshness alarm in `update_health.py` was supposed to catch exactly this, but it crashed at `get_token()` on the same DNS error before reaching `check_pipeline_freshness()`, silencing itself when needed most; (b) the agent's 5/02 DATA draft used a bare-list shape for `news.json` (no `{"players": [...]}` envelope) and the normalizer threw on `payload.get(...)`, gracefully writing the raw payload but losing the injuries-section derivation. Recovery: ran `send_pending_email.py` manually — drained both queued DATA drafts (5/02, 5/03), pushed two data commits, sent the 5/03 email (5/02 email correctly skipped as stale by the date guard added 4/22). Fired the Stanton → Rice swap one-shot (`swap_stanton_to_rice.py`) — plist self-uninstalled, confirmation email sent, commit `7b752f0`. Hardening (commit `8e8fb83`): moved `check_pipeline_freshness()` ahead of any network step in `update_health.py` (it's local-only — reads `data/yesterday.json`, fires a Reminder via osascript) so DNS/OAuth crashes can't silence it; extended `normalize_news` to accept the bare-list shape and derive injuries from per-player `injury_status` fields. Added a 5th "Known failure modes" entry in CLAUDE.md for DNS/network outages at cron time. 5/04 box scores not backfilled — those games hadn't been played yet at outage time.
- **2026-05-01** — Recovered fantasy baseball pipeline after 2-day silent outage. Symptom: today's scheduled remote agent reported success but no email arrived and website still showed Apr 28 data. Two-layer root cause: (a) local Gmail OAuth refresh token revoked 4/30 morning, blocking `send_pending_email.py` from seeing or processing drafts on both 4/30 and 5/1; (b) the daily cron ran fine, the agent kept creating drafts (yesterday's 4/29 pair plus today's three 4/30 attempts including my two manual debug runs) — they just piled up unconsumed in Gmail. Mistakenly chased a `permitted_tools:[]` red herring on the trigger config (patched anyway, harmless); the actual fix was just re-authing Gmail. Re-auth itself broke once mid-debug — concurrent polling raced with the `gmail-personal` MCP server on token refresh, triggering Google's rotation-revocation. Drained backlog: deleted 5 duplicate drafts, ran `send_pending_email.py` to push 4/29 + 4/30 data commits and send the 4/30 email. Backfilled `season_stats.json` from MLB Stats API ground truth for all 15 non-swapped slots (agent's web-sourced totals had been very stale — Cal Raleigh 91→125 AB, Pete Crow-Armstrong 74→116, Teoscar 61→98, Devin Williams +1 W, Eovaldi +1 W) and merged the after-4/29 baseline + 4/30 daily increment for the swapped Lindor/McGonigle slot. Added `check_pipeline_freshness()` to `update_health.py` (3:13am cron): if `data/yesterday.json` watermark is >36h stale, fires a macOS Reminder via the existing `notify_reauth_needed` helper. Tested with fresh / 24h / 48h / missing-date inputs. Saved deferred OAuth-client-separation proposal to memory for next time mid-week revocation hits — concrete recommendation is a new GCP OAuth client (Desktop type) for local scripts, isolating their refresh token from the gmail-personal MCP server's; ~10 min of work.
- **2026-04-29** — Fixed Lindor/McGonigle propagation to the website. Symptom: SS row blank on Team Overview, "Francisco Lindor — DNP, IL" on /yesterday. Root cause: the remote trigger prompt at `trig_01AWGDMAqyJY5oZNYiqKQQdT` was never updated to know about the `current_player` convention introduced 2026-04-27, so the agent kept querying Lindor (still on IL) and writing JSON keyed `"Francisco Lindor"` instead of `"Lindor/McGonigle"` — `team.js`'s strict `statsByName[roster.name]` join missed → blank row. Two-front fix: (1) updated the trigger prompt via `RemoteTrigger` API to define `search_name = current_player or name` (used for all 4 WebSearches per player) and `slot_name = name` (canonical JSON key + season_stats key); for swapped slots, accumulate yesterday's box-score deltas onto the existing carryover (recomputing AVG = H/AB and ERA = ER × 9 / IP) instead of overwriting from a season-totals page that would double-count pre-swap games; added self-check that every JSON `name` matches a roster `slot_name`. (2) Local hot-fix commit `d5ff52a`: renamed `season_stats.json` key Francisco Lindor → Lindor/McGonigle (added McG's 4/28 1-for-4 deltas onto Lindor's 4/26 carryover → AB 87, H 16, AVG .184, HR 2, RBI 5, R 7, SB 0); replaced `yesterday.json` SS entry with McGonigle's actual 4/28 line (1-for-4 vs ATL, Tigers lost 5-2; Tigers off 4/27 so 4/28 is his first game in the slot); recomputed `totals.hitters` AB 26→30, H 9→10; replaced Lindor IL writeup in `news.json` players[] with a McGonigle blurb and removed Lindor from injuries[]. Prompt fix is generic — when the 5/4 Stanton → Rice swap fires, the agent will use `search_name="Ben Rice"` / `slot_name="Stanton/Rice"` without further prompt edits. Updated CLAUDE.md: noted that the trigger prompt is editable via the `RemoteTrigger` API tool (not just the claude.ai web UI), and added a paragraph to the "Mid-season player swaps" section documenting that the prompt now honors `current_player` generically, so future swaps need only the my_team.json + season_stats.json edits.
- **2026-04-28** — Armed a one-shot launchd job (`com.jon.fantasy-baseball-swap`) to swap Giancarlo Stanton → Ben Rice (NYY) in the DH slot at 6:30am ET on 2026-05-04, mirroring the Lindor → McGonigle pattern. Fires 1hr after the 5:30am `send_pending_email` job so May 3 stats are already in `season_stats.json`. Script `swap_stanton_to_rice.py`: pre-flight checks (DH slot still named "Giancarlo Stanton", Stanton key present in season_stats, `yesterday.json.date >= 2026-05-03`, no clobber of an existing "Stanton/Rice" key); applies the rename in-place (`Giancarlo Stanton` → `Stanton/Rice` in season_stats; DH entry replaced with composite form, `current_player: "Ben Rice"`, projected zeroed out per option C); fetch+rebase, commit, push to origin/main; sends a plain-text confirmation email to levinson.jon@gmail.com (CC levinsonlgs@gmail.com) with the carried-forward totals + commit SHA; then `launchctl bootout` + deletes its own plist so it can't fire again next May. Idempotent (already-applied state is detected and exits 0 with self-uninstall). Failure path: if git push fails, files are written but a failure email is sent and the launchd job is *not* removed — Jon must re-run manually. Dry-run today (with the date guard temporarily relaxed) confirmed the proposed diff matches the Lindor/McGonigle precedent. Plist verified loaded via `launchctl print` (Month=5, Day=4, Hour=6, Minute=30). Script committed to git pre-fire for disaster recovery (sleeping Mac on May 4 means missed calendar trigger) and audit trail; safe to leave in history post-success — date guard + idempotency check + self-uninstalled launchd job make accidental re-fire a no-op.
- **2026-04-27** — Fixed Teoscar Hernández stats showing as zeros on the Team Overview page. Root cause: `my_team.json` had the accented form `"Teoscar Hernández"` but the remote agent has been writing every data file (`season_stats.json`, `yesterday.json`, `news.json`) with the no-accent form `"Teoscar Hernandez"` — likely because ESPN/MLB.com search snippets omit the diacritic. `web/assets/team.js:74` does a strict `statsByName[p.name]` lookup, so the join missed and his row contributed nothing to team totals. Other pages were unaffected: `yesterday.js`/`news.js` render directly from the JSON without cross-referencing the roster, and `update_health.py` joins via `normalize_name` against MLB Stats API (which uses the accented form, confirmed in `mlb_player_ids.json` cache key). Fix: flipped his roster `name` to the no-accent form (so it joins cleanly with all agent-produced data) and added `current_player: "Teoscar Hernández"` so the MLB API path keeps matching. Single-file edit, no data files touched.
- **2026-04-27** — Mid-season SS swap: replaced Francisco Lindor with Kevin McGonigle (DET) effective today, carrying Lindor's stats through 4/26 forward. Roster slot relabeled `"Lindor/McGonigle"` with new optional `current_player` field pointing to `"Kevin McGonigle"`; `season_stats.json` pre-seeded with Lindor's 4/26 totals (AB 83, H 15, .181, 2 HR, 5 RBI, 7 R) under the composite key so the remote agent's daily accumulator adds McGonigle's 4/27+ deltas on top — yielding a combined-AB batting average per spec. Wired `current_player` through `run_daily.py` (web-search queries) and `update_health.py` (boxscore matching, MLB ID resolution, ESPN news, injury filter). Removed Season YTD/Pace columns from `update_health.py`'s 3:13am email (and the now-unused `fetch_season_stats`/`resolve_player_ids` calls) — MLB Stats API returns true cumulative stats per real player, which would double-count McG's pre-4/27 games against the carryover; the website (sourced from `season_stats.json`) remains canonical. Set `projected_points: 0` for the slot (option C) since no preseason projection exists for McG. Documented the convention under a new "Mid-season player swaps" section in CLAUDE.md.
- **2026-04-25** — Lindor injury (left calf strain, IL'd 4/23) replacement analysis. Pulled Pitcherlist Top 150 Hitters Week 4, cross-referenced against league players_master.csv for SS eligibility, and ranked options for our scoring formula (BA×1000 + counting stats). Wilson taken before action, so recommendation became Kevin McGonigle (DET, PL #68) — runs-heavy + plate-discipline profile fits our AVG-weighted scoring. Fallbacks: Konnor Griffin, Carlos Correa. JJ Wetherholt ruled out (listed 2B in master file, not SS). Lindor timeline: 3-week shutdown, re-eval mid-May, best-case return late May → 5–8 weeks out.
- **2026-04-22** — Debugged stale-website issue: Gmail OAuth refresh token was revoked Apr 20 (not age-expired), breaking local send_pending_email.py for 2 days while remote agent kept creating unconsumed drafts. Re-authed Gmail, manually processed backlog, reverted an out-of-order Apr 20 commit that had overwritten Apr 21 data. Hardened pipeline (commit `ffc189f`): send_pending_email now parses dates from draft subjects and processes oldest-first, skipping stale drafts at/below the on-disk watermark; OAuth refresh failures fire a persistent macOS Reminder (replacing the Gmail-based warning, which was useless when Gmail itself was the failing credential); update_health's misleading "Daily email sent" log now honestly reports success vs. failure. Fixed JS timezone bug in web/assets/shared.js — `new Date("YYYY-MM-DD")` was rendering "April 20" for Apr 21 data because ISO-date strings parse as UTC midnight. Updated remote trigger prompt (`trig_01AWGDMAqyJY5oZNYiqKQQdT`) to enforce explicit `stats` schema per position type (HITTER/SP/RP) and added a pre-draft self-check — today's agent run had shipped empty `stats: {}` for every player, rendering blank H/AB/R/HR/RBI/SB columns.
- **2026-04-19** — Built web interface + refactored daily pipeline. Added 3-view static site (Team Overview / Yesterday / News) deployed to Vercel at btbaseball.com (apex → www redirect via Squarespace DNS; SVG baseball emoji favicon). Updated daily_report.py to export structured JSON; added yesterday.json + news.json schemas. Migrated season_stats.json schema (store AVG/ERA directly, drop H/BB/ER). Trigger prompt rewritten twice: unified single-draft approach failed with stream idle timeout on 45KB Gmail body; final design splits into two drafts (DATA + Daily) with subject-prefix routing. send_pending_email.py now consumes the DATA draft (extract → write 3 data files → commit+push → delete), then sends the email draft. Added schema normalizer to absorb agent field-name drift. Confirmed remote agent git is genuinely read-only (403 on push proxy even with allow_unrestricted_git_push=true; API-level PAT injection does not work). End-to-end tested: agent created both drafts, local script pushed Apr 19 data, site went live with real data at https://www.btbaseball.com.
- **2026-04-17** — Manually ran April 16 daily report (remote agent produced no draft again). Light Thursday: only 4/16 played (PHI, NYM, LAD, CHC all off). Stanton the star (2-run HR 446 ft in Angels 11-4 win over Yankees, +4 pts). Raleigh 2-for-4 with RBI single off Buehler (SEA swept 5-2). Arraez reached on E6 and scored (Giants 3-0 Reds shutout). Merrill 1-for-4 quiet day but Padres extend streak to 8. All 7 pitchers DNP (no rotation match, Burns IL, Wheeler rehab). Team total: +6.0. Injury updates: Wheeler strong 4th rehab start Apr 14 (5.2 IP, 9 K, 95 mph velocity), one more Sunday Apr 19 before likely late-April return; Burns still on IL (flexor). Season stats updated. Email sent via Gmail API (message ID 19d9c97c35b264c2).
- **2026-04-16** — Manually ran April 15 daily report. All 9 hitters played, only Devin Williams pitched (0.1 IP, 4 ER — grand slam to Rushing, ERA spikes to 8.31). All 6 SP DNP. Teoscar Hernandez best performer (2-4, HR, 2 R, 1 RBI = +4 pts). Jackson Merrill walk-off 2-run double caps Padres' 5-run 9th vs SEA. Harper solo HR in Phillies' 11-2 loss to Cubs. Lindor and Stanton both 0-for-4. 3 injury updates: Arraez (wrist contusion, back in lineup), Wheeler (IL rehab, target Apr 19-24), Ragans (thumb cleared). Season stats updated. Email sent via Gmail API.
- **2026-04-14** — Manually ran April 13 daily report. Light Monday: 8/16 played, only Eovaldi pitched (7 IP, 0 ER, 7 K, W vs OAK — best start of 2026, +3.9 pts). Padres/Giants/Reds/Royals all off. Lindor and Bichette 0-fers in Mets' 0-4 shutout loss at LAD. Stanton and Raleigh each had 2-hit games. Team total: +8.9 pts. 3 injury updates: Arraez day-to-day (R wrist contusion), Wheeler IL rehab (targeting Apr 24-26 return), Ragans day-to-day (L thumb contusion, no fracture). Season stats updated. Email sent via Gmail API.
- **2026-04-13** — Manually ran April 12 daily report. Searched box scores via ESPN for all 16 players. Quiet Sunday: 8/16 played, all 7 pitchers DNP. Jackson Merrill star of the day (2-for-3, HR, SB, +5.0 pts). Team total: +7.0 pts. 3 injury updates: Arraez day-to-day (R wrist contusion, X-rays neg), Wheeler IL rehab (targeting 4/24 return), Chase Burns placed on 15-day IL (R flexor strain). Personal Gmail token expired — created draft via claude.ai connector. Season stats updated.
- **2026-04-10** — Manually recovered April 9 daily report after remote agent failed (Gmail MCP tool not configured + HTML build timed out). Remote agent had updated season_stats.json but never built HTML or created draft. Researched box scores via ESPN, built report locally, created Gmail draft via claude.ai connector. Quiet day: only 4/16 players in action (Bichette 1-3, Lindor 1-4, Merrill 0-4 w/ R, Stanton 0-2). Injury updates: Wheeler rehab start #3 (4.1 IP, 1 ER, on track for ~April 17 return), Ragans day-to-day (thumb contusion, expects next start).
- **2026-04-09** — Diagnosed remote agent failure: Gmail MCP connector disconnected on claude.ai, causing agent to write to pending_draft.json instead of creating Gmail draft (which also can't be pushed due to read-only git). Generated April 8 daily report locally via WebSearch, sent email directly. PCA highlight: 3-for-5, first HR of 2026 (389 ft), 3 R, 1 SB. Ragans injury: left thumb contusion from comebacker, expects to make next start. Updated season_stats.json.
- **2026-04-08** — Set up automated daily report pipeline: remote scheduled agent (4:30am ET) builds report via WebSearch and creates Gmail draft via claude.ai connector; local launchd job (5:30am ET) finds and sends the draft via Gmail API. Pushed project to GitHub (levinsonjon/bt-baseball-2026, public). Tested end-to-end successfully with Apr 7 report.
- **2026-04-07** — Fixed `compute_points()` bug (missing `config.PITCHER_SCORING`): split into hitter/SP/RP branches with correct formulas (SP: daily RSAR contribution, RP: 5×(W+SV)). Added `compute_ytd_points()` and `compute_pace_points()` functions using full league scoring (BA×1000 for hitters, RSAR for SP, 5×(W+SV) for RP). Rewrote `build_html_email()` to match established email format (separate hitter/pitcher tables, Opp/Summary/News/Pre/YTD/Pace columns, DNP rows, injury table). Set up local cron job for 2:07 AM ET nightly reports.
- **2026-04-07** — Resent April 6 daily report with corrected YTD/Pace calculations (full scoring formula including BA*1000). Fixed duplicate Teoscar Hernandez entry in season_stats.json (merged accented/unaccented). Replaced partial season_stats with full 2026 season-to-date cumulative stats for all 16 players from web search.
- **2026-04-07** — Resent April 6 daily report with updated email format
- **2026-04-07** — Ran daily fantasy baseball report for Apr 6. Searched stats for all 16 players via WebSearch. Light day: Mets (Bichette, Lindor, Williams) and Yankees (Stanton) had off days; no SPs pitched. 5 hitters scored points led by Teoscar Hernandez (+8.0, 2-for-5, HR, 4 RBI, SB). Team total: +18.0. Wheeler only injury (IL, rehab). Sent HTML email to levinson.jon@gmail.com (CC: levinsonlgs@gmail.com). Season stats saved. Note: config.py missing PITCHER_SCORING attribute caused run_daily.py to fail; built report manually via DayResult objects.
- **2026-04-06** — Added Gmail token reauth warning to `update_health.py`: checks refresh token expiry after each daily run and sends a Jon-only alert email (no CC) with re-auth command when token expires within 1 day. Added `cc` parameter to `send_alert_email()`.
- **2026-04-06** — Manually ran daily fantasy baseball report for Apr 5. Fetched box scores via MLB Stats API, news via web search, built and sent HTML email to levinson.jon@gmail.com (CC: levinsonlgs@gmail.com).

- **2026-04-04** — Fixed daily email CC: `send_alert_email` in `update_health.py` was not setting a CC header, so `config.REPORT_EMAIL_CC` (`levinsonlgs@gmail.com`) never received the daily report. Added CC header from config.

- **2026-03-30** — Fixed off-by-one column bug in `update_health.py`: reads/writes were targeting columns G:H (Adj Points + Health) instead of H:I (Health + Injury Note). Fixed read range (B2:H → B2:I), column indices (row[5]/row[6] → row[6]/row[7]), and write range (G:H → H:I).

- **2026-03-30** — Fixed name matching in draft tracker: added Unicode accent stripping to _normalize_name and first-initial abbreviation matching to _find_player (e.g., "J Ramirez" now resolves to "José Ramírez")
- **2026-03-30** — Fixed draft recommendations to exclude players at already-filled positions (filtered available list by open roster slots)
- **2026-03-30** — Re-ran health update and full projections push (`push_to_sheets.py`) to restore corrupted Adj Points column and populate correct Health/Injury Note columns.
